{
	"name": "@iris/monitor",
	"version": "0.1.0",
	"scripts": {
		"dev": "watchexec -e rs -r RUST_LOG=info cargo run",
		"build": "cargo build --release",
		"start": "RUST_LOG=info ./target/release/monitor"
	}
}